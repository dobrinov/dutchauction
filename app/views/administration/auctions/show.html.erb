<%= render partial: 'auctions/auction_state',  locals: { auction: @auction } %>
<%= render partial: 'auctions/auction',        locals: { auction: @auction } %>
<%= render partial: 'auction_actions',         locals: { auction: @auction } %>